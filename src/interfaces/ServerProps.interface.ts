/**
 * Server properties for models and inputs
 */
export interface ServerProps {
  enumRef: string;
  isArray: boolean;
  name: string;
  nullable: boolean;
  reference: string;
  schema: string;
  type: string;
}
